# Stop charging at 80% to preserve battery lifespan
# Install: sudo cp ~/.config/udev/rules.d/99-battery-threshold.rules /etc/udev/rules.d/ && sudo udevadm control --reload-rules
SUBSYSTEM=="power_supply", ATTR{id}=="BAT0", ATTR{charge_control_end_threshold}="80"
